# replication.yaml

SOURCE: ORACLE_DEV
TARGET: SNOWFLAKE_DEV

defaults:
  mode: full-refresh
  #object: "{stream_schema}_{stream_table}"

streams:
  SYSADM8.PS_BI_ACCT_ENTRY:
    object: "ARTEMIS.___MIGRATION_STG.PEOPLESOFT_DEV_PS_BI_ACCT_ENTRY"

